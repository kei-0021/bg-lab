# BG-LAB

## 開発者向け

### 開発時

- `pnpm install` の実行中にターミナルを閉じることは辞めてください
  - `node_modules 2` など意図しないパスが作られてしまう可能性があります

### 完成時

- `server.js` を変更した場合は、必ず `pnpm run build` を実行してください
  - 変更を反映するためにはビルドが必須です

- 以下のコマンドでフォーマットをかけてください
  - PR時にフォマットを確認するCIを設定しています

```bash
pnpm prettier --write .
```
